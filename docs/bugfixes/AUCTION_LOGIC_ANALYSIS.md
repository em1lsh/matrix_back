# Анализ логики аукционов

## Дата: 2024-12-02

---

## Проблема

При проверке исправления бага с `expired_at < datetime.now()` на `> datetime.now()` обнаружена дополнительная проблема в фоновой задаче.

---

## Логика аукционов

### Поле `expired_at`
- **Значение:** Дата и время истечения аукциона
- **Тип:** DateTime

### Состояния аукциона

1. **Активный аукцион:** `expired_at > datetime.now()`
   - Аукцион еще не истек
   - Можно делать ставки
   - Должен отображаться в списке

2. **Истекший аукцион:** `expired_at <= datetime.now()`
   - Аукцион завершен
   - Нельзя делать ставки
   - Должен быть обработан фоновой задачей

---

## Найденные баги

### ✅ БАГ 3 (ИСПРАВЛЕН): Эндпоинты возвращали истекшие аукционы

**Файлы:** `backend/project/app/api/routers/auctions.py`

**Было:**
```python
# POST /auctions/ - строка 36
models.Auction.expired_at < datetime.now()  # Возвращало истекшие

# GET /auctions/my - строка 102
models.Auction.expired_at < datetime.now()  # Возвращало истекшие

# POST /auctions/new - строка 132 (проверка существующего)
models.Auction.expired_at < datetime.now()  # Проверяло истекшие

# POST /auctions/bid - строка 196
models.Auction.expired_at < datetime.now()  # Искало истекшие
```

**Стало:**
```python
models.Auction.expired_at > datetime.now()  # Возвращает активные
```

**Обоснование:**
- Пользователи должны видеть только активные аукционы
- На истекшие аукционы нельзя делать ставки
- Истекшие аукционы обрабатываются фоновой задачей

---

### ❌ БАГ 4 (НОВЫЙ): Фоновая задача ищет активные вместо истекших

**Файл:** `backend/project/app/account/account.py:345`

**Текущий код:**
```python
@staticmethod
async def run_auctions_checker():
    """
    Раз в 15 минут завершает истекшие аукционы
    """
    async with SessionLocal() as db_session:
        auctions = await db_session.execute(
            select(models.Auction)
            .where(models.Auction.expired_at >= datetime.now())  # ❌ НЕПРАВИЛЬНО
            .options(joinedload(models.Auction.nft).joinedload(models.NFT.gift),
                     joinedload(models.Auction.user))
        )
```

**Проблема:**
- `expired_at >= datetime.now()` находит аукционы которые **еще не истекли** или **только что истекли**
- Должно быть `expired_at <= datetime.now()` чтобы найти **истекшие** аукционы

**Правильный код:**
```python
auctions = await db_session.execute(
    select(models.Auction)
    .where(models.Auction.expired_at <= datetime.now())  # ✅ ПРАВИЛЬНО
    .options(joinedload(models.Auction.nft).joinedload(models.NFT.gift),
             joinedload(models.Auction.user))
)
```

---

## Почему это критично

### Текущее поведение (с багом):
1. Фоновая задача ищет аукционы где `expired_at >= now`
2. Это включает:
   - Аукционы которые истекают **прямо сейчас** (expired_at == now)
   - Аукционы которые истекут **в будущем** (expired_at > now)
3. Результат: **Активные аукционы могут быть преждевременно завершены!**

### Правильное поведение (после исправления):
1. Фоновая задача ищет аукционы где `expired_at <= now`
2. Это включает только:
   - Аукционы которые уже истекли (expired_at < now)
   - Аукционы которые истекают прямо сейчас (expired_at == now)
3. Результат: **Только истекшие аукционы завершаются**

---

## Риски

### Высокий риск:
- **Преждевременное завершение аукционов:** Активные аукционы могут быть завершены до истечения срока
- **Потеря денег пользователей:** Если аукцион завершается раньше, пользователи теряют возможность сделать ставку
- **Нарушение бизнес-логики:** Аукцион должен работать до указанного времени

---

## Рекомендации

### 1. Исправить БАГ 4
```python
# backend/project/app/account/account.py:345
.where(models.Auction.expired_at <= datetime.now())
```

### 2. Добавить тесты
- Тест на завершение истекших аукционов
- Тест что активные аукционы НЕ завершаются
- Тест на граничные случаи (expired_at == now)

### 3. Добавить логирование
```python
logger.info(f"Processing {len(auctions)} expired auctions")
for auction in auctions:
    logger.info(f"Completing auction {auction.id}, expired at {auction.expired_at}")
```

---

## Выводы

1. ✅ **БАГ 3 исправлен правильно** - эндпоинты теперь возвращают активные аукционы
2. ❌ **БАГ 4 обнаружен** - фоновая задача использует неправильное условие
3. ⚠️ **Критичность:** Высокая - может привести к преждевременному завершению аукционов
4. 🔧 **Требуется:** Исправить условие в `run_auctions_checker()`

---

## Следующие шаги

1. Исправить условие в `run_auctions_checker()`
2. Протестировать фоновую задачу
3. Добавить unit-тесты
4. Добавить логирование для мониторинга
