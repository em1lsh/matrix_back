# Логирование - Быстрый старт

## 🚀 Использование

### Импорт

```python
from app.utils.logger import get_logger

logger = get_logger(__name__)
```

### Базовые примеры

```python
# Информация
logger.info("Пользователь создан")
logger.info(f"NFT {nft_id} куплен пользователем {user_id}")

# Предупреждение
logger.warning("Redis недоступен, используется fallback")

# Ошибка
logger.error(f"Ошибка при покупке NFT: {e}")

# Отладка
logger.debug(f"Блокировка получена: {lock_key}")
```

## 📊 Уровни логирования

| Уровень | Когда использовать | Примеры |
|---------|-------------------|---------|
| **DEBUG** | Детальная отладка | Блокировки, SQL запросы |
| **INFO** | Основные события | Запуск задач, успешные операции |
| **WARNING** | Предупреждения | Fallback режимы, повторы |
| **ERROR** | Ошибки | Исключения, сбои |
| **CRITICAL** | Критические сбои | Остановка системы |

## ⚙️ Настройка

В `.env`:
```env
LOG_LEVEL=INFO              # DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_RETENTION_DAYS=7        # Дни хранения логов
```

## 📂 Файлы логов

```
backend/project/logs/
├── app.log          # Основные логи
├── loki-app.log     # JSON для Loki
└── errors.log       # Только ошибки
```

## 🔍 Просмотр логов

```bash
# Последние логи
tail -f backend/project/logs/app.log

# Только ошибки
tail -f backend/project/logs/errors.log

# Поиск
grep "presale_checker" backend/project/logs/app.log
```

## ✅ Правильно

```python
# С контекстом
logger.info(f"NFT {nft_id} куплен за {price} TON")

# С обработкой ошибок
try:
    await buy_nft(nft_id)
except Exception as e:
    logger.error(f"Ошибка покупки NFT {nft_id}: {e}")
```

## ❌ Неправильно

```python
# Без контекста
logger.info("Операция выполнена")

# Чувствительные данные
logger.info(f"Password: {password}")  # НИКОГДА!

# Избыточное логирование
for item in items:
    logger.debug(f"Processing {item}")  # Слишком много!
```

## 📖 Полная документация

См. `backend/docs/LOGGING.md`
