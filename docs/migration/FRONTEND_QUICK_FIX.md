# ⚡ Быстрые исправления для фронтенда

**Дата:** 6 декабря 2025  
**Статус:** ✅ Все endpoints мигрированы

---

## 🎯 Что нужно сделать

### ✅ Бэкенд готов (100%)
Все 62 endpoints мигрированы с идентичной бизнес-логикой

### ⚠️ Нужно обновить фронтенд
Изменены методы и добавлена пагинация

---

## ⚠️ ИЗМЕНЕНИЯ ДЛЯ ФРОНТЕНДА

### 1. Изменить методы (GET → POST)

Фронтенд нужно обновить:

```javascript
// БЫЛО (GET):
axios.get('/nft/set-price', { params: { nft_id: 123, price: 10 } })

// СТАЛО (POST):
axios.post('/nft/set-price', { nft_id: 123, price: 10 })
```

### Список изменений:
1. `GET /nft/set-price` → `POST /nft/set-price`
2. `GET /nft/buy` → `POST /nft/buy`
3. `GET /nft/deals` → `POST /nft/deals`
4. `GET /market/output` → `POST /market/output`
5. `GET /channels/new` → `POST /channels/new`
6. `GET /accounts/new` → `POST /accounts/new`
7. `DELETE /accounts` → `POST /accounts/delete`

---

### 2. Изменить пути (/trade → /trades)

```javascript
// БЫЛО:
axios.get('/trade/my')
axios.post('/trade/new')
axios.get('/trade/personal')

// СТАЛО:
axios.get('/trades/my')
axios.post('/trades/new')
axios.get('/trades/personal')
```

Все 11 endpoints trades изменили путь для консистентности.

---

## 🟡 ПАГИНАЦИЯ: Обновить структуру

### Было:
```javascript
// Response: [item1, item2, item3]
const items = response.data;
```

### Стало:
```javascript
// Response: { items: [...], total: 100 }
const { items, total } = response.data;
```

### Endpoints с пагинацией:
- `/nft/my`
- `/nft/sells`
- `/nft/buys`
- `/auctions/my`
- `/auctions/deals`
- `/offers/my`
- `/presale/my`
- `/trade/my`
- `/users/topups`
- `/users/withdraws`

---

## 📝 ЧЕКЛИСТ

### Бэкенд:
- [x] ✅ Все 62 endpoints мигрированы
- [x] ✅ Бизнес-логика идентична
- [x] ✅ Добавлены улучшения безопасности

### Фронтенд:
- [ ] Обновить 7 методов GET → POST
- [ ] Изменить пути /trade → /trades (11 endpoints)
- [ ] Обновить 15 endpoints для пагинации
- [ ] Исправить `recived` → `received` в offers
- [ ] Добавить idempotency_key для `/market/output`

---

## 🚀 Быстрый старт

### Для фронтенда:
```bash
# 1. Обновить методы GET → POST (7 endpoints)
# 2. Изменить пути /trade → /trades (11 endpoints)
# 3. Добавить поддержку пагинации (15 endpoints)
# 4. Добавить idempotency_key для вывода средств
```

### Рекомендации:
- Создать API adapter layer для изоляции изменений
- Использовать feature flags для постепенной миграции
- Тестировать на staging перед продакшном

---

## 📚 Подробнее

См. **[FRONTEND_API_MAPPING.md](FRONTEND_API_MAPPING.md)** для полного анализа

---

*Документ создан 6 декабря 2025*
