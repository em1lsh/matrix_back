# Тесты тяжелых эндпоинтов

**Цель:** Зафиксировать текущую бизнес-логику перед рефакторингом на use-cases.

**Статус:** ✅ Готово к запуску

---

## Зачем эти тесты?

Мы планируем рефакторинг тяжелых ручек для оптимизации времени запросов:
- Перевод на use-case архитектуру
- Оптимизация запросов к БД
- Добавление кеширования
- Улучшение индексов

**Эти тесты гарантируют, что бизнес-логика останется неизменной после рефакторинга.**

---

## Покрытые эндпоинты

### 🔥 Market (самые нагруженные)
- `POST /api/market/` - список товаров с фильтрами и пагинацией
- `POST /api/market/patterns` - фильтры паттернов
- `POST /api/market/models` - фильтры моделей
- `POST /api/market/backdrops` - фильтры фонов
- `GET /api/market/collections` - список коллекций
- `GET /api/market/topup-balance` - реквизиты для пополнения
- `GET /api/market/output` - вывод средств (критичная!)

### 👤 Accounts
- `GET /api/accounts` - список аккаунтов
- `DELETE /api/accounts` - удаление аккаунта

### 📢 Channels (очень тяжелые)
- `GET /api/channels` - список каналов на продаже
- `GET /api/channels/my` - мои каналы
- `GET /api/channels/buys` - мои покупки
- `GET /api/channels/sells` - мои продажи
- `GET /api/channels/set-price` - установка цены
- `DELETE /api/channels` - удаление канала

### 🎯 Auctions
- `POST /api/auctions/` - список аукционов
- `GET /api/auctions/my` - мои аукционы
- `POST /api/auctions/new` - создание аукциона
- `GET /api/auctions/del` - удаление аукциона
- `GET /api/auctions/deals` - история сделок

---

## Запуск тестов

### Быстрый запуск

```bash
cd backend
poetry run pytest tests/test_heavy_endpoints.py -v
```

### Запуск конкретного класса

```bash
# Только тесты Market
poetry run pytest tests/test_heavy_endpoints.py::TestMarketEndpoints -v

# Только тесты Accounts
poetry run pytest tests/test_heavy_endpoints.py::TestAccountsEndpoints -v

# Только тесты Channels
poetry run pytest tests/test_heavy_endpoints.py::TestChannelsEndpoints -v

# Только тесты Auctions
poetry run pytest tests/test_heavy_endpoints.py::TestAuctionsEndpoints -v
```

### Запуск конкретного теста

```bash
poetry run pytest tests/test_heavy_endpoints.py::TestMarketEndpoints::test_get_salings_basic -v
```

### С подробным выводом

```bash
poetry run pytest tests/test_heavy_endpoints.py -vv -s
```

---

## Что проверяют тесты?

### ✅ Базовая функциональность
- Эндпоинты возвращают 200 OK
- Структура ответа корректна
- Данные соответствуют ожиданиям

### ✅ Бизнес-логика
- **Фильтрация:** работает корректно (titles, models, patterns, backdrops)
- **Пагинация:** страницы не пересекаются, лимиты соблюдаются
- **Сортировка:** данные отсортированы правильно
- **Валидация:** некорректные данные отклоняются
- **Права доступа:** пользователь видит только свои данные

### ✅ Критичные операции
- **Idempotency:** повторные запросы с одним ключом не дублируют операции
- **Транзакции:** баланс изменяется корректно
- **Каскадное удаление:** связанные данные удаляются правильно
- **Временные ограничения:** проверка давности создания аккаунта

---

## Структура тестов

```
tests/
├── conftest.py                    # Фикстуры (db_session, test_user, client)
├── test_heavy_endpoints.py        # Основные тесты
└── HEAVY_ENDPOINTS_TESTS.md       # Этот файл
```

### Фикстуры

```python
@pytest.fixture
async def db_session():
    """Асинхронная сессия БД"""

@pytest.fixture
async def test_user(db_session):
    """Тестовый пользователь с балансом 10 TON"""

@pytest.fixture
def test_token(test_user):
    """Токен для авторизации"""

@pytest.fixture
async def client():
    """HTTP клиент для запросов"""
```

---

## Примеры тестов

### Тест фильтрации

```python
@pytest.mark.asyncio
async def test_get_salings_with_filters(self, client, test_user, test_token, db_session):
    """POST /api/market/ - фильтрация по коллекциям"""
    # Создаем тестовые данные
    gift = models.Gift(id=999001, title="Test Collection", ...)
    nft = models.NFT(gift_id=999001, price=1000000000, ...)
    
    # Тест с фильтром
    response = await client.post("/api/market/", json={"titles": ["Test Collection"], ...})
    
    # Проверки
    assert response.status_code == 200
    assert all(item["gift"]["title"] == "Test Collection" for item in response.json())
```

### Тест idempotency

```python
@pytest.mark.asyncio
async def test_output_idempotency(self, client, test_user, test_token, db_session):
    """GET /api/market/output - idempotency key работает"""
    # Создаем существующий withdraw
    existing_withdraw = models.BalanceWithdraw(idempotency_key="duplicate_key_001", ...)
    
    # Повторный запрос с тем же ключом
    response = await client.get("/api/market/output", params={"idempotency_key": "duplicate_key_001", ...})
    
    # Проверки
    assert response.json()["idempotent"] is True
    assert test_user.market_balance == 20_000_000_000  # Баланс не изменился
```

---

## Ожидаемые результаты

### ДО рефакторинга
```
tests/test_heavy_endpoints.py::TestMarketEndpoints::test_get_salings_basic PASSED
tests/test_heavy_endpoints.py::TestMarketEndpoints::test_get_salings_with_filters PASSED
tests/test_heavy_endpoints.py::TestMarketEndpoints::test_get_salings_pagination PASSED
...
======================== 30 passed in 15.23s ========================
```

### ПОСЛЕ рефакторинга
```
tests/test_heavy_endpoints.py::TestMarketEndpoints::test_get_salings_basic PASSED
tests/test_heavy_endpoints.py::TestMarketEndpoints::test_get_salings_with_filters PASSED
tests/test_heavy_endpoints.py::TestMarketEndpoints::test_get_salings_pagination PASSED
...
======================== 30 passed in 8.45s ========================  ⚡ FASTER!
```

**Все тесты должны проходить ДО и ПОСЛЕ рефакторинга!**

---

## Следующие шаги

### 1. Запустить baseline тесты
```bash
poetry run pytest tests/test_heavy_endpoints.py -v > tests/results/baseline_heavy_endpoints.txt
```

### 2. Выполнить рефакторинг
- Перевести на use-case архитектуру
- Оптимизировать запросы к БД
- Добавить кеширование
- Улучшить индексы

### 3. Запустить тесты после рефакторинга
```bash
poetry run pytest tests/test_heavy_endpoints.py -v > tests/results/optimized_heavy_endpoints.txt
```

### 4. Сравнить результаты
```bash
diff tests/results/baseline_heavy_endpoints.txt tests/results/optimized_heavy_endpoints.txt
```

**Все тесты должны проходить!**

---

## Troubleshooting

### Проблема: Тесты падают с ошибкой подключения к БД
**Решение:** Проверьте что `ENV=test` установлен и `.env.test` настроен правильно

### Проблема: Тесты падают из-за существующих данных
**Решение:** Фикстура `clean_db` должна очищать данные автоматически. Проверьте `conftest.py`

### Проблема: Тесты проходят локально, но падают в CI
**Решение:** Убедитесь что тестовая БД создана и миграции применены

---

## Метрики

### Покрытие
- **Market:** 8 тестов
- **Accounts:** 3 теста
- **Channels:** 7 тестов
- **Auctions:** 6 тестов
- **Всего:** 24 теста

### Критичность
- 🔴 **Критичные:** 5 тестов (output, buy_channel, bid, idempotency)
- 🟡 **Важные:** 12 тестов (фильтрация, пагинация, транзакции)
- 🟢 **Базовые:** 7 тестов (списки, удаление)

---

## Заключение

Эти тесты - **страховка** перед рефакторингом. Они гарантируют что:
- ✅ Бизнес-логика не сломается
- ✅ API контракты останутся неизменными
- ✅ Критичные операции работают корректно
- ✅ Можно безопасно оптимизировать код

**Запускайте тесты перед каждым коммитом во время рефакторинга!**
